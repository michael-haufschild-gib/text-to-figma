/// <reference types="vitest/globals" />

/**
 * Global type declarations for Vitest test environment.
 * Enables TypeScript to recognize Vitest globals (describe, it, expect, vi, etc.)
 * when running type-check on test files.
 */

export {};
