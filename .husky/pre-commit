#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fail fast on any error
set -e

# Run lint-staged for code formatting and linting
npx lint-staged

# Run type checking via root workspace command
echo "Running TypeScript type check..."
npm run type-check

echo "✓ Pre-commit checks passed"
